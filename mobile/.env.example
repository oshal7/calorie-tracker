# URL of the deployed backend worker (no trailing slash), e.g. https://calorie-tracker-backend.your-subdomain.workers.dev
EXPO_PUBLIC_API_BASE_URL=

# Must match the APP_SHARED_SECRET set on the backend (wrangler secret put APP_SHARED_SECRET)
EXPO_PUBLIC_APP_SHARED_SECRET=
